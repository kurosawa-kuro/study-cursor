# 03. Cursor 操作リファレンス（Hobby 枠 + Chat / Composer / Tab / Inline Edit）

～ Cursor の主要機能の使い分けと、Hobby 枠（無料プラン）で快適に使うための運用ノウハウ ～

> 📌 本ファイルは利用者が **詰まったときに引くリファレンス**。最初から通読する必要はありません。
> 初回セットアップは [`00_クイックスタート.md`](./00_クイックスタート.md) または [`01_環境構築ガイド.md`](./01_環境構築ガイド.md)。

---

## 1. Cursor Hobby 枠の制約

### 1.1 Hobby 枠とは

Cursor の **無料プラン**。新規アカウントは自動的に Hobby 枠から始まる。月単位で **slow request** を一定数使える。

### 1.2 月次上限（公式 docs を確認して数値を埋める）

> ⚠️ Cursor の料金プラン・上限値は変動するため、最新の値は公式 docs を確認してください。
> 公式: https://cursor.com/pricing / https://docs.cursor.com/

| 項目 | Hobby 枠の値 |
|---|---|
| Slow request 月次上限 | `<公式値を確認して記入>` |
| Fast request 月次上限 | `<公式値を確認して記入>`（Hobby は基本 slow のみのことが多い） |
| 利用可能モデル | `<公式値を確認して記入>` |
| Composer / Background Agent の制限 | `<公式値を確認して記入>` |

### 1.3 Hobby 枠で快適に使うコツ

- **1 リクエスト 1 タスク** に絞る（複合タスクは小さく分割）
- **長文を 1 回で投げない**（プロンプトメモのセクション単位でコピペ）
- **多並列で Chat を立ち上げない**（同時実行は意味が薄く、上限を消費するだけ）
- `@file` / `@folder` を使って **AI に読ませる範囲を明示**（暗黙にリポジトリ全体を読まれると重い）
- 上限に近づいたら **待機**（月初にリセット）、または **Pro への移行** を検討（本キット外の判断）

### 1.4 上限に達したときの症状と対処

| 症状 | 対処 |
|---|---|
| `Too many slow requests` / `You've used all your slow requests` | 月初までの待機。緊急なら Pro 検討 |
| Chat の応答が極端に短くなる / 質が落ちる | フォールバックモデルに切り替わっている可能性。モデル選択 UI を確認 |
| Composer がプレミアム機能と表示される | Hobby では使えない範囲。Chat / Inline Edit で代替 |

---

## 2. Chat (Ctrl+L) — 基本

### 2.1 何に使うか

- 質問・調査
- 小さなコード片の生成
- 既存ファイルの説明・要約
- テキスト整形（メール文面、議事録、表形式変換 など）

### 2.2 使い方

1. `Ctrl+L` を押す（または右上の Chat アイコン）
2. 右ペインに Chat 欄が表示される
3. 入力欄にプロンプトを書いて Enter

### 2.3 コンテキスト追加（`@` 記法）

Chat に投げる前に **どのファイル / フォルダを参照させるか** を明示できる。

| 記法 | 効果 |
|---|---|
| `@file` | ファイル単体を選んで添付 |
| `@folder` | フォルダ単位で添付（中の全ファイルがコンテキストに） |
| `@docs` | Cursor の Indexed Docs（公式ドキュメント等を事前登録したもの）を参照 |
| `@web` | Web 検索を絡める（Hobby 枠で使える範囲を確認） |
| `@git` | コミット履歴を参照 |

事務処理タスクの例（ファイル名は意図的に曖昧。中身は `@file` で読んで判別する）:

```
@file src/office-task/data/新規 データ1.csv の内容を読み、まずこのファイルが何のデータかを 1 行で要約し、続いて主力スキル別の人数と月単金の平均を Markdown 表で出してください。
```

```
@folder src/office-task/data の txt ファイル群（中身は契約書とスキルシート）を読み、それぞれの要点を 1 行で要約し、推奨ファイル名案も併せて提示してください。
```

---

## 3. Composer (Ctrl+I) — 複数ファイル横断の変更

### 3.1 何に使うか

- 複数ファイルにまたがるリファクタリング
- 新規ファイル + 既存ファイル修正を 1 回で行うタスク

### 3.2 Hobby 枠での注意

Composer は **Hobby 枠だと制限がある** ことがある（公式 docs で最新仕様を確認）。本キットの事務処理タスクは基本的に Chat で完結するため、**Composer は無理に使わない方針**。

---

## 4. Inline Edit (Ctrl+K) — ピンポイント編集

### 4.1 何に使うか

- エディタで開いているファイルの **選択範囲だけ** AI に書き換えてもらう
- 「この行を Markdown 表にして」「この段落を敬語にして」など局所的な編集

### 4.2 使い方

1. 編集したい範囲を選択
2. `Ctrl+K`
3. 指示を入力 → Enter
4. 差分プレビューが出るので、`Accept` で反映、`Reject` で取り消し

---

## 5. Tab 補完

### 5.1 何に使うか

- 入力中の **次の数行**を AI が予測して提示
- そのまま採用するなら `Tab` キー
- 不要なら `Esc` または入力を続ける

### 5.2 Hobby 枠での扱い

Tab 補完は Hobby 枠でも基本的に使える（公式 docs を確認）。事務処理タスクでは Markdown や CSV を編集中に **次のセル / 項目** を予測してくれるので有用。

---

## 6. Rules（`AGENTS.md` と `.cursor/rules/*.mdc`）

### 6.1 仕組み

Cursor は以下を **Chat / Composer のコンテキストに自動付与** する:

- リポジトリルートの `AGENTS.md`
- `.cursor/rules/*.mdc` 配下のルールファイル

本キットには両方とも同梱済み。ユーザーが明示的に `@file` で貼る必要はない。

### 6.2 本キットで適用されるルール（要約）

- **ランタイム非インストール原則**: Python / Node.js のインストールを伴う解を提案しない
- **Windows 標準機能優先**: PowerShell / Excel / エクスプローラ / Edge で完結する解を案内
- **元ファイル不変更**: ファイル操作タスクではコピー版を新名で保存
- **日本語回答**: ドキュメント・回答はすべて日本語

詳細は [`../../AGENTS.md`](../../AGENTS.md) と [`../../.cursor/rules/repo.mdc`](../../.cursor/rules/repo.mdc) を参照。

### 6.3 ルールが効いていないと感じたら

- Chat 上部に「Rules applied」「Applied rules」のような表示があるか確認
- 無ければ Open Folder を一度閉じて開き直す
- それでも効かないなら、Chat に明示的に `AGENTS.md のルールに従って再回答してください` と追記

---

## 7. 事務処理タスクでよく使う組み合わせ

| やりたいこと | 推奨 Cursor 機能 | 補助 |
|---|---|---|
| サンプル CSV を要約して欲しい | Chat + `@file` | — |
| データフォルダ全体の中身を一覧化 | Chat + `@folder` | — |
| Markdown 議事録のテンプレ作成 | Chat | — |
| 既存テキストを敬語に書き換え | Inline Edit (Ctrl+K) | — |
| ファイル整理のための PowerShell スクリプト生成 | Chat | 生成後にエディタで保存し、PowerShell で手元実行 |
| Excel 数式 / Power Query の手順書 | Chat | Excel 側で手順を再現 |

---

## 8. トラブル時の参照先

- Cursor インストール / サインイン: [`01_環境構築ガイド.md`](./01_環境構築ガイド.md) 末尾「トラブルシューティング」
- リポジトリを開く / Chat に投入: [`02_ハンズオン.md`](./02_ハンズオン.md) 末尾「トラブルシューティング」
- Hobby 枠の最新の上限値・モデル: https://cursor.com/pricing / https://docs.cursor.com/
- Cursor の `@` 記法: https://docs.cursor.com/context/@-symbols/overview
- Cursor の Rules: https://docs.cursor.com/context/rules-for-ai
